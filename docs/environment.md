# Variáveis de ambiente

Copie `.env.example` para `.env.local` e use as credenciais públicas do mesmo projeto Firebase do Atlas.

Não coloque `serviceAccountKey.json` nem chaves privadas no projeto do site.
